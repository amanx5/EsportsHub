# ==============================
# Development Environment Config
# ==============================

# VITE_ prefixed vars → exposed to browser code (React). Example: import.meta.env.VITE_API_URL
# Non-prefixed vars → available only to Node (e.g. inside vite.config.js if you load them with loadEnv('', '', '')), not bundled into frontend.

# Base API URL for backend services in development
VITE_API_URL=http://localhost:3000

# Resource endpoint (e.g. for images, files, etc.)
VITE_RESOURCE_URL=http://localhost:3000/resource

# Show technical errors details on UI
VITE_SHOW_ERROR_DETAILS=false